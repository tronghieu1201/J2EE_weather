This directory is intended to store the pre-trained XGBoost models.

As discussed in the project notes, these models should be trained (e.g., using Python and Scikit-learn/XGBoost) and then saved.




Place the saved model files here, for example:
- daily_model.json
- hourly_model.json
